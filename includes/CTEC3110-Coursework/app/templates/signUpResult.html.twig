{% extends 'banner.html.twig' %}
{% block content %}
    <main>
        <div class="header-container">
            <h2 class="display-4">
                {{ page_heading_2 }}
                <small class="text-muted">{{ page_heading_1 }}</small>
            </h2>
        </div>
        <div class="alert alert-danger" role="alert">{{ result }}</div>
        <br/>
        <a class="btn primary-color" href="{{ landing_page }}">Back to
            Messages</a>
        <a class="btn primary-color" href="{{ path_for(auth_page) }}">{{ auth_text }}</a>
    </main>
{% endblock %}