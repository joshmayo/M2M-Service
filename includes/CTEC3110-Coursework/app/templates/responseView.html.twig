{% extends 'banner.html.twig' %}
{% block title %}Error{% endblock %}
{% block content %}
    <main>
        <div class="header-container">
            <h2 class="display-4">
                {{ page_heading_2 }}
                <small class="text-muted">{{ page_heading_1 }}</small>
            </h2>
        </div>
        <h4>{{ error_msg }}</h4>
        <br />
        <a class="btn btn-primary primary-color button" href="{{ path_for(admin_dash) }}">Return to dashboard</a>
    </main>
{% endblock %}