{% extends 'banner.html.twig' %}
{% block title %}Send Message Result{% endblock %}
{% block content %}
    <main>
        <div class="header-container">
            <h2 class="display-4">
                {{ page_heading_2 }}
                <small class="text-muted">{{ page_heading_1 }}</small>
            </h2>
        </div>
            <div class="alert alert-danger" role="alert">{{ message }}</div>
            <br />
            <a class="btn btn-primary primary-color button" href="{{ landing_page }}">Back to Messages</a>
            <a class="btn btn-primary primary-color button" href="{{ path_for(sendMessage_page) }}">Send another
                Message</a>
        </div>
    </main>
{% endblock %}