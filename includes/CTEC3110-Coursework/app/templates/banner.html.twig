{% extends 'layout.html.twig' %}
{% block banner %}
    <nav class="navbar navbar-expand-lg navbar-dark">
        <a class="navbar-brand" href="{{ landing_page }}">CTEC3110</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item navbar-item">
                    <a class="nav-link navbar-link" href="{{ landing_page }}">View Messages</a>
                </li>
                <li class="nav-item navbar-item">
                    <a class="nav-link navbar-link" href="{{ path_for(sendMessage_page) }}">Send Message</a>
                </li>
                <li class="nav-item navbar-item">
                    <a class="nav-link navbar-link" href="{{ path_for(analytics_page) }}">Analytics</a>
                </li>
            </ul>
            <span class="navbar-text">
                <a href="{{ path_for(login_page) }}" class="btn btn-sm primary-color">Login</a>
                <a href="{{ path_for(SignUp_page) }}" class="btn btn-sm primary-color">Sign up</a>
            </span>
        </div>
    </nav>
{% endblock %}